function toggleMode() {
  const html = document.documentElement

  html.classList.toggle('light')

  // const img = document.querySelector("#profile img")

  // if(html.classList.contains('light')) {
  //   img.setAttribute("alt", "foto de Elíria usndo tranças jaqueta jeans sorrindo com uma mão no queixo")
  // } else {
  //    img.setAttribute("alt", "se tivesse outra foto pro mode dark")
  // }
}